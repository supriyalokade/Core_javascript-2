// Arrow function to calculate the square of a number
const square = (num) => num * num;

// Example usage: calculating the square of 5
const result = square(5);

// Display the result
console.log("The square of 5 is:", result);
